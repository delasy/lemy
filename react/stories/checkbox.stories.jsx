import React from 'react'

import { Checkbox } from '../index'

export const Default = (args) => {
  return (
    <Checkbox {...args} />
  )
}

export default {
  component: Checkbox,
  title: 'Components/Checkbox'
}
