import React from 'react'

import { Input } from '../index'

export const Default = (args) => {
  return (
    <Input {...args} />
  )
}

export default {
  component: Input,
  title: 'Components/Input'
}
