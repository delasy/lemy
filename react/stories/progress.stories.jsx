import React from 'react'

import { Progress } from '../index'

export const Default = (args) => {
  return (
    <Progress {...args} />
  )
}

export default {
  component: Progress,
  title: 'Components/Progress'
}
